/**
 * Animation utilities — Motion for UI transitions, GSAP for scroll-driven storytelling.
 * Imported here so the foundation is ready; not used until product UI is built.
 */
export { gsap } from "gsap";
export { ScrollTrigger } from "gsap/ScrollTrigger";
export { motion, AnimatePresence } from "motion/react";
